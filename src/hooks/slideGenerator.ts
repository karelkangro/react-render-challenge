// utils/slideGenerator.ts
import { AIGeneratedSlide } from '@/types';
import { mockSlides } from '@/utils/mockData';

export function generateSlidesFromInput(): AIGeneratedSlide[] {
  return mockSlides;
}
